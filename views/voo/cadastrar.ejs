<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastrar Voo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 600px;
            margin-top: 50px;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #343a40;
            margin-bottom: 30px;
        }

        .form-control {
            margin-bottom: 15px;
        }

        .btn-submit {
            width: 100%;
        }

        .btn-back {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <%- include("../partials/cabecalho.ejs") %>

    <div class="container">
        <h1>Cadastrar Voo</h1>
        <form action="/voos" method="post">
            <input type="hidden" name="_id" value="<%= voo._id %>">

            <div class="mb-3">
                <label for="numerovoo" class="form-label">Número do Voo</label>
                <input type="text" class="form-control" id="numerovoo" name="numerovoo" value="<%= voo.numerovoo %>" required>
            </div>

            <div class="mb-3">
                <label for="trajeto" class="form-label">Trajeto</label>
                <input type="text" class="form-control" id="trajeto" name="trajeto" value="<%= voo.trajeto %>" required>
            </div>

            <div class="mb-3">
                <label for="partida" class="form-label">Partida</label>
                <input type="datetime-local" class="form-control" id="partida" name="partida" value="<%= voo.partida %>" required>
            </div>

            <button type="submit" class="btn btn-primary btn-submit">Salvar</button>
        </form>

        <div class="text-center btn-back">
            <a href="/voos" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Voltar para Listagem
            </a>
        </div>
    </div>

    <%- include("../partials/rodape.ejs") %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.js"></script>
</body>
</html>
